DATABASE_URL=postgres://postgres:MOT_DE_PASSE@localhost:5432/gameboy_configurator
# Base dédiée aux tests d'intégration (recommandé : éviter de mélanger données de test et dev).
# Créer la base : createdb gameboy_configurator_test
# DATABASE_URL_TEST=postgres://postgres:MOT_DE_PASSE@localhost:5432/gameboy_configurator_test
# Auth (Story 3.0) — obligatoire en prod. Si défini, le cookie auth est Secure (HTTPS uniquement).
# En dev sans JWT_SECRET, le cookie est envoyé sur http://localhost.
JWT_SECRET=votre-secret-jwt-long-et-imprevisible-min-32-caracteres
# CORS — origine explicite pour cookies (pas *)
CORS_ORIGIN=http://127.0.0.1:5173
